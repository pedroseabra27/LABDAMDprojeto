name: shared_core
description: Core models and services shared across LDAMD flutter apps.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.6.0
  socket_io_client: ^3.1.5

dev_dependencies:
  lints: ^2.0.0
